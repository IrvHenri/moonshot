describe("Portfolio", () => {
  beforeEach(() => {
    cy.visit("/");
    // add steps for logging in or signing up
    // navigate to portfolio route
  });
  it("should add a  coin", () => {});
  it("should edit a coin ", () => {});
  it("should remove a coin", () => {});
});
